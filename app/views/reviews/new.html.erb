<% if @restaurant.errors.any? %>
The form has the following errors:
	<% @restaurant.errors.full_messages.each do |message| %>
		<%= message %>
	<% end %> 
<% end %> 


Enter review for <%= @restaurant.name %>

<%= form_for [@restaurant, @review] do |f| %>
<%= f.label :thoughts %>
<%= f.text_area :thoughts, placeholder: 'Enter your thoughts' %>

<%= f.label :rating %>
<%= f.select :rating, (1..5) %> 

<%= f.submit %>

<% end %> 

